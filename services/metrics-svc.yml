apiVersion: v1
kind: Service
metadata:
  name: metrics
spec:
  selector:
    name: metrics
  type: NodePort
  ports:
    - name: "http"
      port: 8080
      targetPort: 8080
    - name: "prometheus"
      port: 9090
      targetPort: 9090
      nodePort: 31031
